import React from 'react'
import {
    StyleSheet,
    Text,
    View,
    TouchableHighlight,
    NativeEventEmitter,
    NativeModules,
    Platform,
    PermissionsAndroid,
    ScrollView,
    AppState,
    FlatList,
    Dimensions,
    Button,
    SafeAreaView,
    StatusBar,
} from 'react-native';

export default function Home() {
    return (
        <View>
            <Text>This is the home screen</Text>
        </View>
    )
}
